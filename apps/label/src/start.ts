/**
 * TanStack Start Instance Configuration
 *
 * qino-label is a local-only tool without authentication.
 * No middleware needed.
 */

import { createStart } from "@tanstack/react-start";

export const startInstance = createStart(() => {
  return {};
});
