# Conversation data (large, private)
data/corpus/

# Database and backups (contain conversation references)
corpus.db
backups/

# Doctor sidecar — written by the MCP server on startup so the user can
# read the latest health check; rewritten on every server start.
corpus-doctor.txt

# Generated metadata
metadata.json
.ingest_state.json

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/

# Node
node_modules/

# Build and cache
.turbo/
dist/
.next/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# macOS
.DS_Store
